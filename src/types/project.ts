type Project = {
  $collectionId: string;
  $createdAt: string;
  $databaseId: string;
  $id: string;
  $permissions: any[]; // Adjust the type accordingly if there is a specific structure for permissions
  $updatedAt: string;
  name: string;
};
